@model IEnumerable<PageContent>
@{
    ViewData["Title"] = "Nábor";
}

@foreach (var item in Model.Where(p => p.SectionName == "Nábor" || p.SectionName == "nábor"))
{
    <h3>@item.Title</h3>
    @Html.Raw(item.Content)
    if (User.IsInRole("admin"))
    {
        <a href="/Page/Edit/@item.Id">Edit</a>
    }
}
