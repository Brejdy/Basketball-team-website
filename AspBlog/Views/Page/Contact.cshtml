@model IEnumerable<PageContent>
@{
    ViewData["Title"] = "Kontak";
}

@foreach (var item in Model.Where(p => p.SectionName == "Kontakt" || p.SectionName == "kontakt"))
{
    <h3>@item.Title</h3>
    @Html.Raw(item.Content)
    if (User.IsInRole("admin"))
    {
        <a href="/Page/Edit/@item.Id">Edit</a>
    }
}